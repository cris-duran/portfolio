services:
  portfolio-dev:
    image: portfolio-dev
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 8083:8083
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
