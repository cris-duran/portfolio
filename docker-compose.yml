services:
  portfolio-dev:
    image: portfolio-dev
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 8082:8082
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
